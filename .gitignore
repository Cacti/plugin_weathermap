configs/**
output/**

# The plugin has no Composer manifest of its own; it uses the toolchain the
# base Cacti install already carries in include/vendor.
/vendor/
composer.lock

# Tooling caches and generated reports
.phpunit.cache/
.phpunit.result.cache
.php-cs-fixer.cache
/coverage/
